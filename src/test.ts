function calculaArea(base: number, altura: number): number {
    return base * altura;
}
console.log(calculaArea(10, 34));

function saldacao(nome: string, sobrenome: string): string {
    nome = "Everton";
    sobrenome = "Cadelone";
    return ('Olá' + nome + " " + sobrenome);
}

console.log(calculaArea);







